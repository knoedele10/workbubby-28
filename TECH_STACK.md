# TECH_STACK.md — 「今日热搜」网站 · 技术选型

> 版本：v1.0 ｜ 日期：2026-09-19 ｜ Day 5 · 第 1 周
> 项目：今日热搜（仓库 workbubby-28）｜ 依据：`PRD.md` v1.0、`research.md` v2
> 本文档只做选型和给理由，不写代码。

## 1. 选型原则

1. **零基础能懂**：每个部件都要能在遇到问题时自己排查
2. **免费**：学习项目不产生任何费用
3. **够用就好**：只满足 PRD 的 6 条功能需求，不为了学新东西引入复杂度
4. **可替换**：每个选择都准备一个备胎（见下表「备选」列）

## 2. 选型总览

| 层次 | 选择 | 备选方案 | 为什么选它 |
|---|---|---|---|
| 页面结构 | **原生 HTML + CSS + JavaScript**（无框架、无构建工具） | React / Vue | PRD 只有 2 个页面、6 条功能，框架是杀鸡用牛刀；原生零依赖零构建，出错好排查；AGENTS.md 里 Day 1 就约定了这个栈；教程最丰富 |
| 数据来源 | **60s API**（GitHub 开源项目，MIT 协议）的公共接口：`/v2/weibo`、`/v2/zhihu`、`/v2/douyin` | ① 自建爬虫 ② 付费聚合 API ③ 其他免费接口（如 vvhan API） | 免费开源、接口统一返回 JSON、三个平台一个项目全覆盖；自建爬虫要对抗反爬（research.md 已论证放弃）；付费 API 违背零成本原则 |
| 数据刷新 | 浏览器定时器每 30 分钟触发一次拉取 + 手动刷新按钮 | 引入状态管理库 / 服务端推送 | 对应 FR3，浏览器本地定时足够；不引入任何库 |
| 部署 | **GitHub Pages**（仓库已在 GitHub，推送即发布） | Vercel / Netlify（免费额度） | 不新增账号和流程；免费、静态页完美支持；国内访问速度若不理想再切换 Vercel（迁移成本为零，因为都是纯静态） |
| 版本管理 | **Git + GitHub**（现有仓库 knoedele10/workbubby-28） | — | Day 2 已建好，沿用 |
| 开发工具 | WorkBuddy（AI 结对）+ 本机编辑器 | — | 现有环境，Day 1 已验收 |

## 3. 关键技术风险与预案

| 风险 | 影响 | 预案 |
|---|---|---|
| 60s API 是否允许网页跨域调用（CORS）**待核实** | 若不允许，浏览器直接 fetch 会被拦 | Day 6 第一次写代码时先实测；不行则换 60s 的其他公共实例，或自部署（项目支持 Docker / Cloudflare Workers，仍免费） |
| 60s API 主域名在 Cloudflare，国内直连可能不稳 | 榜单加载慢或失败 | FR5 的异常兜底 + 重试按钮本来就要做；必要时换公共实例或自部署到国内可达平台 |
| 免费接口的请求频率限制 | 被限流导致拉取失败 | 30 分钟刷新一次的频率本身极低；手动刷新按钮加最小间隔即可 |
| GitHub Pages 国内访问速度波动 | 打开慢 | 纯静态站迁移 Vercel 零成本，属可接受的随时可换项 |

## 4. 明确不选（与 PRD 砍功能一一对应）

- **Node.js 后端 / 服务器**：PRD 无登录、无存储、无自建爬虫 → 纯静态页足够
- **数据库**：不保存任何用户数据和历史榜单 → 无处可用
- **前端框架与构建工具（React/Vue/打包器）**：2 个页面不需要组件化和工程化 → 徒增学习成本
- **付费服务（API / 托管 / 监控）**：零成本原则

## 5. 与 PRD 功能需求的对应关系

| PRD 功能 | 由哪个选型满足 |
|---|---|
| FR1 榜单展示 | 原生 HTML/CSS 渲染 + 60s API 三个榜单接口 |
| FR2 跳转原文 | 榜单数据自带原文链接，浏览器新标签页打开 |
| FR3 数据刷新 | 浏览器定时器（30 分钟）+ 手动刷新按钮 |
| FR4 更新时间 | 前端记录最近一次成功拉取的时间并显示 |
| FR5 异常兜底 | 前端对每个榜单独立请求、独立提示失败与重试 |
| FR6 来源标注 | 静态页脚文案 |

## 6. 待核实清单

1. 60s API 的 CORS 支持（Day 6 第一件事实测）
2. 60s API 公共实例列表与频率限制（以官方文档为准：https://github.com/vikiboss/60s）
3. GitHub Pages 在当前网络下的实际访问速度（部署后实测）

---

*技术选型 v1.0 · 2026-09-19 · 作者：同学（AI 代写，本人确认）*
