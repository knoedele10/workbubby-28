# TECH_DESIGN.md — 「今日热搜」网站 · 技术设计与数据流

> 版本：v1.0 ｜ 日期：2026-09-20 ｜ Day 5 · 第 1 周
> 项目：今日热搜（仓库 workbubby-28）｜ 依据：`PRD.md` v1.0、`TECH_STACK.md` v1.0
> 本文档只讲「技术怎么分工、数据怎么流」，不写代码。

## 1. 一句话数据流（今日掌握）

> 数据从各平台公开榜单来（经免费开源的 60s API 转交），浏览器拉取后渲染成榜单页面，用户点击条目跳回原平台看原文；**全程只在浏览器里过一手，不落库、不保存、不转载**。

## 2. 前后端与数据库分工

### 2.1 通用分工（先讲概念）

| 角色 | 通俗理解 | 典型职责 |
|---|---|---|
| 前端 | 用户看得见、点得着的**门面** | 展示界面、响应用户操作、向服务器要数据 |
| 后端 | 看不见的**后厨** | 处理业务规则、读写数据、对外提供接口 |
| 数据库 | 后厨的**储藏室** | 长期保存数据（用户、订单、历史记录等） |

### 2.2 本项目的分工（为什么只有前端）

| 角色 | 本项目怎么办 | 理由 |
|---|---|---|
| 前端 | **承担全部工作**：拉数据、渲染榜单、跳转原文、失败提示 | PRD 的 6 条功能全部是「展示 + 跳转」，浏览器一个人干得过来 |
| 后端 | **没有** | 无登录、无自建爬虫、无业务规则 → 没有活给它干（PRD 砍功能时已论证） |
| 数据库 | **没有** | 不存用户、不存历史榜单，30 分钟刷新一次永远拿「最新一份」→ 没东西可存 |

> 有人替我们干了后端的活：60s API 这个开源项目把「抓取各平台榜单」这个后端工作做成了免费公共接口。我们不重复造这个轮子，只做好门面。

## 3. 技术路线（一行）

> **纯静态网页（原生 HTML/CSS/JS）→ 浏览器 fetch 60s API 三个榜单接口 → 渲染成榜单 → 部署到 GitHub Pages，用户点击跳转原平台。**

为什么走这条路线（详见 TECH_STACK.md）：零基础可维护（无框架无构建）、零成本（接口与托管全免费）、每个环节都有备胎（接口可换实例，托管可迁 Vercel）。

## 4. 数据流图

```
 ┌─────────────────┐                        ┌─────────────────────┐
 │   用户浏览器      │  ① 请求榜单数据         │    60s API（免费开源） │
 │  （纯前端，无后端） │ ────────────────────→  │  /v2/weibo          │
 │                 │  ② 返回榜单 JSON         │  /v2/zhihu          │
 │                 │ ←────────────────────   │  /v2/douyin         │
 └───────┬─────────┘     （排名/标题/链接）     └──────────┬──────────┘
         │                                              │ ⓪ 各平台榜单
         │ ③ 渲染榜单页面                                  │   数据由它定时抓取
         ▼                                              ▼
 ┌─────────────────┐                        ┌─────────────────────┐
 │   榜单页面        │                        │  微博 / 知乎 / 抖音    │
 │ 排名 + 标题 + 链接  │  ④ 点击条目，新标签页      │  （原平台公开榜单）     │
 │ 最近更新时间       │ ────────────────────→  │  用户在此看原文        │
 │ 失败提示+重试按钮   │      跳转原平台原文        └─────────────────────┘
 └─────────────────┘

 数据边界：数据只在「浏览器 ←→ 60s API」之间流动一次，不落库、不保存、不转载正文。
```

图上四个箭头对应 PRD 功能：①③ 支持 FR1/FR3/FR4/FR5，② 提供数据，④ 实现 FR2；来源标注 FR6 是页面静态文案。

## 5. 余力加练：方案变化时哪些文件受影响

| 如果……发生变化 | 受影响文件 |
|---|---|
| 换数据源（60s API 换成别的接口） | `TECH_STACK.md`、`TECH_DESIGN.md`、将来的榜单拉取代码（Day 6+） |
| 部署从 GitHub Pages 换 Vercel | `TECH_STACK.md`、`TECH_DESIGN.md`（代码零改动，纯静态） |
| 增加平台榜单（百度 / B站 / 头条） | `PRD.md`（FR1）、`TECH_STACK.md`、`TECH_DESIGN.md`、首页页面代码 |
| 榜单增加热度值等字段展示 | `PRD.md`（FR1）、页面渲染代码 |
| 未来要做登录 / 订阅 / 历史 | PRD、TECH_STACK、TECH_DESIGN 全部重估，且需要新增后端与数据库 |

## 6. 小课堂：开发一个网页/App 会用到哪些技术

| 技术 | 是什么 | 优势 | 劣势 |
|---|---|---|---|
| HTML | 页面的骨架，决定「有什么内容」 | 简单、必学 | 只管结构，不负责好看和交互 |
| CSS | 页面的皮肤，决定「长什么样」 | 直观、见效快 | 兼容性细节多 |
| JavaScript | 页面的神经，决定「会做什么动作」 | 浏览器原生支持，能直接调接口 | 原生写复杂应用会乱，所以才有人发明框架 |
| 前端框架（React/Vue） | 帮你组织大量 JS 代码的脚手架 | 大型应用好维护 | 要学构建工具，小项目是负担 |
| 后端语言（Node.js/Python 等） | 后厨：处理业务和数据 | 能做登录、存储等前端做不了的事 | 要服务器、要运维，成本高 |
| 数据库（MySQL/MongoDB 等） | 储藏室：长期存数据 | 数据不丢、可查询 | 需要设计和维护，无存储需求时是累赘 |
| API | 服务之间的**窗口**：按约定格式交换数据 | 复用别人做好的能力（如本项目用 60s API） | 依赖对方稳定性，需要兜底 |
| 部署托管（GitHub Pages/Vercel） | 把网页放到公网让人访问 | 免费、推送即更新 | 纯静态为主，带后端就要换方案 |

## 7. 待核实清单（继承自 TECH_STACK.md）

1. 60s API 的 CORS 跨域支持（Day 6 第一件事实测）
2. 60s API 公共实例列表与频率限制（官方文档：https://github.com/vikiboss/60s）

---

*技术设计 v1.0 · 2026-09-20 · 作者：同学（AI 代写，本人确认）*
